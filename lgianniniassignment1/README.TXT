If we want to call the program with only its name do:
export PATH = $PATH:./

Also to have it print the list of load averages:

example:
	observer -s 10 600
This will run the program and report a system load average every 10 seconds for 600 seconds.

